@model IEnumerable<MvcCreditApp1.Models.Bid>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Создать заявку", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreditHead)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.bidDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreditHead)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.bidDate)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.BidId }) |
            @Html.ActionLink("Детали", "Details", new { id=item.BidId }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.BidId })
        </td>
    </tr>
}

</table>
